<%= form_for(@article, html: { class: "form" }) do |f| %>
	<div class="form-field">
		<%= f.text_area :content, placeholder: "compose new article here" %>
	</div>

	<%= f.submit "Post", class: "btn btn-primary" %>
<% end -%>