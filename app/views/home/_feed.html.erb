<% if user_signed_in? %>
	<% if feed_render %>
		<ul class="row">
			<%# render @feed_items %>
			<% feed_render.each do |feed| %>
				<%= link_to feed %> <br>
			<% end -%>
		</ul>
		<%= will_paginate @feed_items %>
	<% end -%>
<% else %>

<% end -%>