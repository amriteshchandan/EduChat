<% if user_signed_in? %>
	<% if user_render %>
		<ul class="row">
			<% user_render.each do |user| %>
				<%= image_tag "coder.jpg" , { size: '50', class: "img-thumbnail" }  %>
				<%= link_to user, user_profile_path(user) %> <br><br>
			<% end -%>
		</ul>
		<%# will_paginate @may_know_user %>
	<% end -%>
<% else %>

<% end -%>